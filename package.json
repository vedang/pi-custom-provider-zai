{
  "name": "pi-custom-provider-zai",
  "version": "0.1.0",
  "description": "Register Cerebras-backed and ZAI-backed GLM models as a pi provider.",
  "type": "module",
  "keywords": ["pi-package", "pi", "extension", "provider", "zai", "cerebras"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "files": ["src/index.ts", "src/core.ts"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc --noEmit --watch --preserveWatchOutput",
    "test": "vitest run --config vitest.config.unit.ts",
    "test:watch": "vitest --config vitest.config.unit.ts",
    "test:coverage": "vitest run --config vitest.config.unit.ts --coverage"
  },
  "peerDependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@mariozechner/pi-ai": "^0.57.1",
    "@mariozechner/pi-coding-agent": "^0.57.1",
    "@types/node": "^24.12.0",
    "@vitest/coverage-v8": "^3.2.4",
    "jscpd": "^4.0.8",
    "knip": "^5.86.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
